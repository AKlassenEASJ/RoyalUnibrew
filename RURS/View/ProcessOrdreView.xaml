<Page
    x:Class="RURS.View.ProcessOrdreView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RURS.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:RURS.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

<Page.DataContext>
        <viewModel:ProcessOrdreViewModel/>
    </Page.DataContext>

    <Grid>
        <StackPanel Orientation="Horizontal" Margin="4,0,-10,0">
            <StackPanel Orientation="Vertical" MaxWidth="500">
                <Button  Content="Indlæs ProcessOrdrer" Command="{Binding LoadCommand}"/>
                <ListView 
                    ItemsSource="{Binding DisplayProcessOrdres, Mode=TwoWay}" 
                    SelectedItem="{Binding SelectedProcessOrdre, Mode=TwoWay}"
                >
                </ListView>
            </StackPanel>
            <StackPanel Orientation="Vertical" MaxWidth="500">
                <TextBlock Text="Opret ny processordrer" FontSize="20"/>
                <TextBox Header="Processordre Nummber" Text="{Binding OpretningProcessOrdre.ProcessOrdreNr, Mode=TwoWay}"/>
                <TextBox Header="Færdigvare Nummer" Text="{Binding OpretningProcessOrdre.FaerdigVareNr, Mode=TwoWay}"/>
                <DatePicker Header="Dato" Date="{Binding OpretningProcessOrdre.Dato, Mode=TwoWay}"/>
                <Button Content="Gem Ordre"  Command="{Binding UploadCommand}"/>
                <ListView 
                    ItemsSource="{Binding OpenOrdreDisplay, Mode=TwoWay}" />
            </StackPanel>
        </StackPanel>
    </Grid>
    <Page.BottomAppBar>
        <AppBar FlowDirection="LeftToRight" IsOpen="True" IsSticky="true">
            <StackPanel Orientation="Horizontal">
                <AppBarButton Icon="play" Label="Åben Ordre" Command="{Binding OpenCommand}">
                </AppBarButton>
                <AppBarButton Icon="save" Label="Gem Ordre" Command="{Binding UplaodCommand}">
                </AppBarButton>
        </StackPanel>
    </AppBar>
    </Page.BottomAppBar>
</Page>